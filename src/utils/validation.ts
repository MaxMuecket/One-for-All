export const isMainPasswordValid = (mainPassword: string): boolean => {
  return mainPassword === '123';
};

// export const servicePasswordValid = (password: string): boolean => {
//   return password ===
// }
